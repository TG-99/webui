version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: keepalive-web
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    restart: unless-stopped
 